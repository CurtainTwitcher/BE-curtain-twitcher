{
  "name": "BE-curtiain-twitcher",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run seed:test && NODE_ENV=test mocha ./spec/*",
    "start": "NODE_ENV=development nodemon index.js",
    "seed:test": "psql -f ./db/setup.test.sql && psql -f ./db/seed.test.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^4.0.1",
    "supertest": "^3.0.0"
  },
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.2"
  }
}
